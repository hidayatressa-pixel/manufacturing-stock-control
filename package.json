{
  "name": "manufacturing-stock-control",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "concurrently -n API,WEB -c cyan,green \"npm run dev -w @msc/api\" \"npm run dev -w @msc/web\"",
    "build": "npm run build -w @msc/web && npm run build -w @msc/api",
    "typecheck": "npm run typecheck -w @msc/web && npm run typecheck -w @msc/api",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate deploy",
    "db:seed": "tsx prisma/seed.ts"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "prisma": "^6.16.2",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2"
  },
  "dependencies": { "@prisma/client": "^6.16.2", "argon2": "^0.44.0" }
}
